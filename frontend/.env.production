# Production Environment Settings
REACT_APP_API_BASE_URL=https://foiyfoshi-backend.onrender.com/api
REACT_APP_ENV=production

# Disable ESLint warnings being treated as errors in production builds
DISABLE_ESLINT_PLUGIN=true

# Generate source maps for debugging (optional)
GENERATE_SOURCEMAP=false

# API endpoint for production
REACT_APP_API_URL=https://foiyfoshi-backend.onrender.com

# Environment
NODE_ENV=production
